[{
  "requirements": [
    { "bin": "rcert", "install": "git clone https://github.com/rix4uni/rcert.git --depth 1 && cd rcert && chmod +x rcert && mv rcert /usr/bin/ && cd .. && rm -rf rcert" },
    { "bin": "unew", "install": "go install github.com/rix4uni/unew@latest" }
  ],
  "command": [
    "rcert -list ${INPUT} | unew -el -i -t ${OUTPUT}"
  ],
  "example": "garudrecon workflow rcert -i domain.com.subs -o domain.com.rcert",
  "description": "Scan certificate transperency with rcert"
}]