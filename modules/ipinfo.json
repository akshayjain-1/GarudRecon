[{
  "requirements": [
    { "bin": "curl", "install": "apt install curl -y" }
  ],
  "command": [
    "curl -s ipinfo.io/${INPUT} > ${OUTPUT}"
  ],
  "example": "garudrecon workflow ipinfo -i json -o output.ipinfo",
  "description": "Test all workers fine"
}]