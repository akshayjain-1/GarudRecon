[{
  "requirements": [
    { "bin": "findomain", "install": "wget -q https://github.com/Findomain/Findomain/releases/download/10.0.1/findomain-linux.zip && unzip -q findomain-linux.zip && rm -rf findomain-linux.zip && chmod +x findomain && mv findomain /usr/bin/" },
    { "bin": "unew", "install": "go install github.com/rix4uni/unew@latest" }
  ],
  "command": [
    "findomain -t ${INPUT} -q | unew -el -i -t ${OUTPUT}"
  ],
  "example": "garudrecon workflow findomain -i domain.com -o domain.com.findomain",
  "description": "Scan subdomain enumeration with findomain"
}]