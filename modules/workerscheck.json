[{
  "requirements": [
    { "bin": "curl", "install": "apt install curl -y" }
  ],
  "command": [
    "while read -r url;do curl -s -I \"$url\" | tee ${OUTPUT};done < ${INPUT}"
  ],
  "example": "garudrecon workflow workerscheck -i subs.txt -o subs.output",
  "description": "Testing all fleet workers fine"
}]